# Fix report: migration 004 — 2026-07-28

Fix-pass over `REVIEW_schema_004_postgres.md` (2 BLOCKER, 8 SHOULD-FIX) and
`REVIEW_schema_004_evaluation.md` (9 BLOCKER, 6 SHOULD-FIX). All 11 blockers and all 14
should-fixes addressed; NITs fixed where trivial, with two deliberate rejections and three
deferrals recorded below.

**Method.** `004_evaluation.up.sql` + `.destructive.down.sql` edited **in place** (the option the
evaluation reviewer recommended: 004 had never held a row, rh-db is single-operator). The live DB
was cycled `down --allow-destructive --target 000` → `up`, so the recorded checksum matches the
new text. The in-place decision is recorded in the migration header. The stale
`-- migrate: non-destructive` directive lines were removed from 001–003's up bodies in the same
cycle (the eval review's coordination note; ADR-002 declares them dead, and the full re-up
re-recorded all checksums).

**Cross-cutting change.** The reviews' shared diagnosis — *recorded intentions, not enforced
invariants* — drove the shape of every fix: each documented guarantee is now a constraint,
trigger, or REVOKE, and the two claims that could not be fully enforced (row-level debate input
provenance; calendar-bound marks) are now explicitly labelled future work in the header and table
comments instead of being implied as done.

---

## Disposition — Postgres correctness review

| ID | Sev | Disposition | Note |
|---|---|---|---|
| B-1 | BLOCKER | **FIXED** | `fk_paper_portfolios_debate`, `fk_paper_portfolios_proposal`, `fk_prd_portfolio`, `fk_evaluation_runs_portfolio` → **RESTRICT** (the reviewer's suggested matrix). Proposals/judgments keep CASCADE from debates = the failed-debate cleanup path; a debate that ever produced a portfolio refuses deletion, and a portfolio with marks/metrics refuses deletion. Pinned by `test_debate_cascade_cannot_destroy_scored_history` (both directions: scored refuses, unscored cleans up). |
| B-2 | BLOCKER | **FIXED** | `ck_paper_portfolios_closed` now `closed_at >= (inception_date::timestamp AT TIME ZONE 'UTC')` — 003's immutable idiom. Pinned by `test_portfolio_close_check_is_timezone_independent` (rejects under both UTC and Pacific/Kiritimati; accepts a legitimate close under Kiritimati). |
| F-3 | SHOULD-FIX | **FIXED** | Shape CHECK rewritten: every non-counterfactual kind (blind, real, agent_composite) pins `debate_id`/`proposal_id` NULL. Tested (blind-with-debate rejected). |
| F-4 | SHOULD-FIX | **FIXED** | `uq_agent_proposals_identity UNIQUE (id, debate_id, agent_id)` + composite `fk_paper_portfolios_proposal (proposal_id, debate_id, agent_id)` — exactly the reviewer's remedy. Cross-attribution tested (bear portfolio on bull's proposal → FK violation). |
| F-5 | SHOULD-FIX | **FIXED** | `cumulative_return`/`total_return`/`annualized_return` → NUMERIC(20,8); `sharpe`/`sortino`/`avg_win_loss`/`information_ratio`/`reward_total` → NUMERIC(18,6). `daily_return` kept (12,8) per the review. Deviation: `volatility` kept (12,8) — annualised vol of fractional daily returns is bounded far below 10^4 (a 39.7% *daily* stdev annualises to ~6.3), so (18,x) would only mask an upstream unit error; it gained a `>= 0` CHECK instead (N-4). |
| F-6 | SHOULD-FIX | **FIXED** | Added: `ix_paper_portfolios_debate`, `ix_paper_portfolios_proposal` (partial — equality implies the predicate, so RESTRICT lookups qualify), `ix_kb_debate`/`ix_kb_portfolio`/`ix_kb_evaluation`, `ix_app_security`, and `ix_prd_source` with a 003-style documented rationale. All FKs on the new tables are indexed too (judgments' two outcome links, guardrail_events ×3, holdings security, rates source). Need was established by the reviewer's EXPLAIN on ~3k-row ANALYZEd tables; per Bar §4.4 the plans should be re-checked once real marks exist (see eval N8). |
| F-7 | SHOULD-FIX | **FIXED** | `updated_at` + shared trigger on `agents`, `debates`, `paper_portfolios` — and on the new mutable tables (`paper_portfolio_positions`, `market_calendar`). |
| F-8 | SHOULD-FIX | **FIXED** | `REVOKE UPDATE, DELETE` on `evaluation_runs`, `portfolio_returns_daily`, `agent_proposals`, `agent_proposal_positions`, `judgments` (+ new history tables). `test_evaluation_runs_are_append_only` keeps its coexistence assertion **and** now asserts `has_table_privilege` false — the name finally tells the truth. See eval B7 for the column-grant design. |
| F-9 | SHOULD-FIX | **FIXED** | `ck_prd_return` → `daily_return >= -1`; comment now states the marked-to-zero case explicitly. The comment and the predicate agree. |
| F-10 | SHOULD-FIX | **FIXED** | `uq_paper_portfolios_one_open ON (kind) WHERE kind IN ('blind','real') AND closed_at IS NULL` — the reviewer's exact idiom — plus `uq_paper_portfolios_one_composite` for the new per-agent composites. Tested incl. close-then-reopen. |
| N-1 | NIT | **REJECTED (kept)** | Redundant `ck_debates_scope` kept deliberately for error readability; now carries a comment saying so, so the redundancy reads as chosen rather than accidental. The reviewer called it harmless. |
| N-2 | NIT | **FIXED** | `COMMENT ON TABLE` for debates, agent_proposals, judgments, paper_portfolios — and every new table. |
| N-3 | NIT | **FIXED** | Via eval S4: `benchmark_symbol` replaced with `benchmark_security_id` FK + `ck_evaluation_runs_ir_bench`. |
| N-4 | NIT | **FIXED** | `volatility >= 0`, `avg_win_loss >= 0`, and `ck_evaluation_runs_n_window` (`n <= window days`) — the upper sanity bound, which also backstops the B6 trigger. |
| N-5 | NIT | **FIXED** | `ck_debates_status_completed`: `(status = 'running') = (completed_at IS NULL)`. |
| N-6 | NIT | **FIXED** | The stale `DROP TRIGGER` line is gone from the down file (the kb trigger no longer exists — S3); table drops carry their triggers. Down now also drops 004's four trigger functions, and the no-residue property is pinned (`pg_proc LIKE 'enforce\_%'` = 0 after down to 000). |
| P-1…P-6 | PRAISE | **PRESERVED** | `uq_agents_one_blind`/`one_real` untouched (retire-then-replace now *tested*); every new CHECK keeps the explicit `IS NULL OR` guard discipline (P-2); `sharpe_n`/`sortino_n` kept and extended, not replaced; the hot-path indexes kept; the NOT NULL as-of anchors kept and now bounded; ADR-002 filename convention unchanged. |

## Disposition — evaluation-framework review

| ID | Sev | Disposition | Note |
|---|---|---|---|
| B1 | BLOCKER | **FIXED** | `risk_free_annual NOT NULL`, `mar_annual NOT NULL DEFAULT 0`, `periods_per_year NOT NULL DEFAULT 252`, `return_frequency NOT NULL DEFAULT 'daily'` (all CHECKed) on `evaluation_runs`, plus the `risk_free_rates` PIT table (known_at in the PK, UTC-anchored known-after-effective CHECK, RESTRICT source FK) — the reviewer's sketch, made concrete. A stored ratio is now self-describing. |
| B2 | BLOCKER | **FIXED** | `judgments.chosen_proposal_id` with a **composite FK `(chosen_proposal_id, debate_id)`** so the backed proposal must belong to the same debate (NO ACTION, not RESTRICT — deliberately, so the failed-debate cascade that deletes proposals *and* judgments in one statement stays legal; the FK comment explains this); `ck_judgments_chosen` (buy/sell must name a proposal); `resulting_portfolio_id` → the book the ruling produced. `ck_paper_portfolios_shape` still bars the real book from a `debate_id` — the reviewer called that modelling defensible *if* something else carries the link, and `resulting_portfolio_id` is that something. Join tested unambiguous (exactly one row per judgment). |
| B3 | BLOCKER | **FIXED** | `paper_portfolio_positions` (lots: entry/exit/reason/realized P&L, exit-pair CHECKs) + `paper_portfolios.cash`, making `market_value` recomputable; deferred constraint trigger `trg_app_weight_sum` caps the per-proposal weight SUM at 100 with a message naming the proposal and the sum (Bar §7.2 loudness). Two deviations from the sketch: `shares > 0` not `>= 0` (a zero-share lot records nothing), and the ≥10% cash floor is deliberately NOT a schema constraint — it is a tunable guardrail whose breaches land in `guardrail_events`; `sum <= 100` is arithmetic, not policy. Both reasons are in the migration comments. |
| B4 | BLOCKER | **FIXED** | 4a `ck_prd_mark_window` (mark within `[trade_date, trade_date+4)` UTC — the reviewer's grace); 4b `trg_prd_inception` (no pre-inception marks) + a guard in `trg_paper_portfolios_inception` refusing `inception_date` moves under existing marks; 4c `ck_debates_context` (`context_as_of <= started_at`); 4d `trg_paper_portfolios_inception` (counterfactual inception ≥ debate cutoff, UTC-anchored inside plpgsql too). The header's "leakage audit is a query" claim is **tempered**: bounds are enforced, row-level input provenance (`debate_inputs`) is explicitly labelled future work in the header and EVALUATION_FRAMEWORK §4 — per the reviewer's "necessary, not sufficient". |
| B5 | BLOCKER | **FIXED** | `ck_evaluation_runs_reward_weights`: reward_total requires all four §5 keys present and numeric (subsumes the reviewer's separate `<> '{}'` check — a 4-key object cannot be empty); `reward_config_version` added. `guardrail_events` created per the sketch, with `rule_key` grammar, severity/action vocabularies, `inputs` JSONB, and `ck_guardrail_events_override` (an override must carry `override_by`) — the ~$4k lesson as durable, queryable state. |
| B6 | BLOCKER | **FIXED** | `trg_evaluation_runs_n` verifies the claimed n against the actual non-NULL mark count for the window (AFTER constraint trigger, so the arithmetic CHECKs fire first; 'daily' rows only — weekly/monthly are derived series, documented). n ≥ 2 gates extended to `hit_rate`, `information_ratio`, `max_drawdown`, `annualized_return`. Policy floor: `min_n_for_ranking NOT NULL` (**no default, deliberately** — a schema default would itself be a hardcoded policy; the writer states the floor in force) + `is_rankable` GENERATED. Tested with the reviewer's exact shape (claim 29 against 9 actual). |
| B7 | BLOCKER | **FIXED** | The REVOKE block, plus column-level lifecycle grants: `debates` UPDATE(status, completed_at); `agents` UPDATE(retired_at, display_name, notes), DELETE revoked ("retired, never deleted" is now mechanism); `paper_portfolios` UPDATE(closed_at, cash); holdings UPDATE(exit columns only); `judgments` UPDATE(resulting_portfolio_id) only — the outcome link is settable after the book exists, the ruling is immutable. kb is fully append-only (S3). Privilege matrix asserted in the append-only test. |
| B8 | BLOCKER | **FIXED** | `split NOT NULL DEFAULT 'live'` (train/validation/test/live), `experiment_id`, `fold_index`, plus `ck_evaluation_runs_fold_exp` (a fold outside an experiment is meaningless — small addition beyond the sketch). |
| B9 | BLOCKER | **FIXED** | New kind `agent_composite`: ONE standing, rebalanced book per persona (`uq_paper_portfolios_one_composite`), making persona-vs-blind the same kind of object; `strategy_mode NOT NULL` (`buy_and_hold`/`rebalanced`) labels the v1 sleeve model honestly; the shape CHECK keeps counterfactuals strict rather than relaxing them (the reviewer offered both options; the new kind avoids weakening F-3). Not enforced: cross-book `base_value` equality (no cross-row CHECK exists; ratios are unaffected; flagged here rather than silently dropped). |
| S1 | SHOULD-FIX | **FIXED** | Same fix as postgres F-4 (identical finding). |
| S2 | SHOULD-FIX | **FIXED** | `agents UNIQUE (id, kind)` + GENERATED kind columns (`proposer_kind = 'persona'`, `judge_kind = 'judge'`, `agent_kind = CASE kind …`) + composite FKs — the reviewer's preferred generated-column approach, **verified live on PG16 before adopting** (constant and CASE generation expressions both enforce on the referencing side with RESTRICT). Tested: persona-judgment, judge-proposal, and persona-owned blind book all refused. |
| S3 | SHOULD-FIX | **FIXED** | Option A (the reviewer's stated preference, consistent with 003's restatement precedent): kb is append-only — `updated_at` + trigger removed, `supersedes_id` self-FK chain with a partial-unique successor index, REVOKE UPDATE/DELETE. Leakage-by-edit is unrepresentable. |
| S4 | SHOULD-FIX | **FIXED** | `benchmark_security_id BIGINT REFERENCES securities` + `ck_evaluation_runs_ir_bench`; `benchmark_symbol` dropped. The suggested `benchmark_returns_daily` table was **not** added: SPY-style benchmarks are securities with bars in 002, and a blended-benchmark table is speculative until one exists (YAGNI; revisit when a non-security benchmark is actually configured). |
| S5 | SHOULD-FIX | **FIXED** | `ck_evaluation_runs_inputs_window` (`inputs_as_of >= window_end` UTC-anchored) — the reviewer's exact constraint. |
| S6 | SHOULD-FIX | **FIXED (binding deferred)** | `market_calendar` created with the session-shape CHECK. The FK/trigger binding from `portfolio_returns_daily` is deliberately deferred until the calendar loader exists — binding marks to an unpopulated calendar would hard-block the marking pipeline, which is the guardrail anti-pattern. The deferral is documented in the table comment, so it reads as chosen, not forgotten. |
| N1 | NIT | **FIXED** | Via F-5 widening + the `annualized_return` n-gate (B6 part 2). |
| N2 | NIT | **FIXED** | Same as postgres N-4. |
| N3 | NIT | **FIXED** | Via F-9; the daily_return column comment now documents NULL for the zero-denominator day. |
| N4 | NIT | **DEFERRED** | A slate ruling is an allocation, which `decision` cannot and should not encode as a scalar — it needs a judgment-allocation payload designed alongside slate-debate support. Deferring to that feature rather than inventing a vocabulary now. |
| N5 | NIT | **FIXED** | `debates.question` and `agent_proposals.rationale` NOT NULL (the two columns the finding names). `judgments.rationale` left nullable — not named, and a hold/escalate ruling's rationale requirement is a policy call for the judgment-payload work (N4). |
| N6 | NIT | **DEFERRED** | No honest natural key exists — several lessons per (debate, portfolio) are legitimate. S3's supersedes chain removes the correction-duplicate motive; a semantic dedupe key waits until entry_type semantics settle with real usage. |
| N7 | NIT | **REJECTED (kept)** | `((kind))` kept over `((true))`: with F-10, `uq_paper_portfolios_one_open` genuinely keys on `kind` for two kinds in one index, and the agents indexes stay consistent with it. The reviewer conceded the current form is correct. |
| N8 | NIT | **DEFERRED** | `EXPLAIN (ANALYZE, BUFFERS)` on `ix_prd_date` requires real marks; no data exists yet. Process note carried in F-6's disposition. |
| PRAISE | — | **PRESERVED** | `context_as_of` NOT NULL (now also bounded), versioned agents, relational positions, `daily_return` unit docs, the n-gate shape, `max_drawdown` sign CHECK, the shape CHECK (strengthened, not removed), and the down migration's honest data-loss statement (updated for the new tables, still honest). |

**Coordination items:** issue #21 (dashboard denominator) remains out of scope and still flagged
in the `agent_proposal_positions` comment. The eval review's "what is missing" list beyond the
numbered findings (orders/fills log, `debate_inputs` provenance, adjustment-history versioning)
is real and remains open — none was a numbered finding; the first two are now named as future
work in the migration header/framework doc so they cannot silently read as done.

---

## Revert-test evidence

Every blocker's regression test was proven to catch its bug: the specific defence was reverted on
the migration (pristine copy kept; file restored after each run), the named test executed, and
failure (RED) confirmed. Harness: scratchpad `revert_proof.py`.

| Blocker | Reverted defence | Named test | Result |
|---|---|---|---|
| PG B-1 | every `ON DELETE RESTRICT` → `CASCADE` (the original chain) | `test_debate_cascade_cannot_destroy_scored_history` | RED |
| PG B-2 | UTC-anchored close CHECK → bare `closed_at::date` | `test_portfolio_close_check_is_timezone_independent` | RED |
| EV B1 | `risk_free_annual` NOT NULL → nullable | `test_evaluation_ratio_parameters_are_recorded` | RED |
| EV B2 | `ck_judgments_chosen` → `CHECK (true)` | `test_judgment_outcome_join_path` | RED |
| EV B3 | weight-sum trigger guard → `IF false` | `test_proposal_weight_sum_and_holdings` | RED |
| EV B4 | `ck_prd_mark_window` upper bound → `true` | `test_lookahead_bounds_enforced` | RED |
| EV B5 | reward-weights CHECK guard → `true OR (…)` | `test_reward_needs_weights_and_guardrails_have_a_home` | RED |
| EV B6 | n-verification trigger guard → `IF false` | `test_n_observations_is_verified` | RED |
| EV B7 | `REVOKE UPDATE, DELETE …` → `REVOKE DELETE …` | `test_evaluation_runs_are_append_only` | RED |
| EV B8 | `ck_evaluation_runs_split` → `CHECK (true)` | `test_walk_forward_split_is_labelled` | RED |
| EV B9 | shape CHECK standing-book branch un-pinned | `test_blind_and_personas_are_comparable_books` | RED |

All 11 RED with the revert; all green on the pristine file.

## Gates (all green, final state)

```
$ .venv/bin/ruff check backend/app src db
All checks passed!

$ .venv/bin/python -m pytest -q
204 passed in 30.41s            # 194 before this pass; 10 net new tests

$ shellcheck -x bin/*.sh
(clean)

$ bash bin/db_migrate.sh down --allow-destructive --target 000 && bash bin/db_migrate.sh up
rolled back 004→001, applied 001→004 (004 in 325ms)

$ bin/db_migrate.sh status
001–004 applied, checksums ok

live verification: schema_migrations = 4 · 0 data rows across all 13 evaluation tables ·
has_table_privilege(rh_app, evaluation_runs, UPDATE/DELETE) = f/f ·
has_column_privilege(rh_app, judgments.resulting_portfolio_id, UPDATE) = t
```

rh-db left healthy: all migrations applied, every table empty, no containers/volumes/networks
outside the rh stack touched, `data/market/` untouched.

---

## Correction pass — untrue directive/comparability claims

Scoped correction applying the re-review's BLOCKER-1, the EV B9 residual (SHOULD-FIX-3), and a
sweep for sibling untrue claims. Independent of the prior fix-pass.

### 1. BLOCKER-1 — the directive/checksum claims (docs only)

Verified first: `grep -rn "migrate:" db/migrations/` finds **no** directive line in any of the
eight migration files. Both false passages then corrected to that verified truth:

- **`db/migrate.py` (docstring, DESTRUCTIVE CLASSIFICATION section)** — no longer claims the
  directives are "still present in the applied 001-003 up bodies". Now states: none remain in
  the tree; the dead lines were removed during the 004 fix-pass, which cycled the schema-only,
  never-shipped database to 000 and re-applied it, re-recording all four checksums; and the
  checksum rule itself is unchanged — editing an applied migration without re-applying it still
  raises `ChecksumMismatch`.
- **`docs/adr/ADR-002-destructiveness-from-filename.md` (Consequences)** — the bullet that
  asserted the lines were "left in place deliberately" now records the reversal explicitly: the
  keep-in-place decision was reversed during the 004 fix-pass (2026-07-28), why the edit was safe
  here (single-operator database, schema only, no row ever written, full 000→up cycle
  re-recorded all four checksums, `status` verified `ok`), and that the invariant stands for any
  database that has shipped data. The reversal now lives in the ADR that owns the decision, not
  only in a fix report.

Nothing was papered over: the checksum mechanism did exactly its job (the pre-cycle `status`
during this very pass showed 004 `MISMATCH` after the schema edit below, and refused to apply
over it until the cycle re-recorded it).

### 2. EV B9 residual — "Composites are marked 'rebalanced'" made TRUE (schema change)

Chose enforcement over rewording, per the re-review's own SHOULD-FIX-3 sketch — the CHECK is
additive, the tables are empty, and a reworded comment would have left the incomparable object
(a composite scored as buy-and-hold) as the *default* insert. `004_evaluation.up.sql` edited in
place:

- New `ck_paper_portfolios_strategy_kind`:
  `(kind <> 'agent_composite' OR strategy_mode = 'rebalanced') AND (kind <> 'counterfactual' OR
  strategy_mode = 'buy_and_hold')`. blind/real deliberately unconstrained (the control's marking
  model is a modelling choice); a composite INSERT must now say `'rebalanced'` explicitly since
  the column default remains `'buy_and_hold'`.
- The `strategy_mode` comment now says the tie is ENFORCED and names the constraint.
- Live cycle: `down --allow-destructive --target 000` → `up`; `status` = 001–004 applied,
  checksums `ok`. Verified live: the constraint exists with the exact definition above, and a
  default-mode composite INSERT is refused by name (probe inside a rolled-back transaction).

**Regression test** — `test_blind_and_personas_are_comparable_books` extended: a composite at
the default mode and a counterfactual claiming `'rebalanced'` both refused with
`match="strategy_kind"` (pinning the constraint, not a confounded neighbour — every other column
in those rows is valid), and a default-mode counterfactual accepted. **Proven RED**: with the
CHECK reverted to `CHECK (true)` on the repo file (pristine copy in scratchpad, restored
byte-identical afterwards, `diff` clean), the test fails `DID NOT RAISE CheckViolation` at the
default-mode-composite assertion (`db/tests/test_runner_db.py:1204`).

### 3. Sweep — scope and findings

Grepped `db/migrations/*.sql`, `db/migrate.py`, `docs/adr/*.md` (both ADRs) for assertive
statements about the migration-file convention, `-- migrate:` directives, checksums, and
portfolio/row comparability (`directive`, `-- migrate`, `checksum`, `comparab`, plus a full read
of the migrate.py docstring and ADR-002). Tested each hit:

- **Fixed (same defect shape):**
  - `004` `base_value` comment ("same notional so returns are comparable") — reworded to the
    truth: parity is a DEFAULT, not schema-enforced; ratio metrics are unaffected; equal
    notionals for absolute-P&L reads are the writing code's responsibility. (Re-review NIT-3;
    enforcement rejected — a cross-row equality CHECK is not expressible, and freezing the
    notional into a per-row CHECK would hard-code policy.)
  - `db/tests/test_runner_db.py:2` module docstring "001-003" → "001-004" (re-review NIT-5; the
    cycle covers 004, and this pass touched the file).
  - `TESTS.md` §3b "currently 104" → 117 (verified by `--collect-only`) and "001-003" →
    "001-004" with the evaluation-schema assertions named (re-review SHOULD-FIX-5; this pass
    changed the suite's coverage, so leaving the stale count would have widened the falsehood).
- **Checked, true as written, left alone:** `migrate.py:18-19` and ADR-002's Context/round
  history (past-tense records of the predecessor design); ADR-002's rename bullet
  ("verified live: 001–003 applied" — a dated verification record from before 004 existed);
  `004`'s REVISION NOTE (re-review confirmed); `004:825` evaluation_runs comparability comment
  (true since B1 — parameters are NOT NULL on the row); `004:693` (an argument, not an
  enforcement claim); `ck_paper_portfolios_strategy` vocabulary comment.
- **Out of scope, deliberately untouched:** the `ck_prd_mark_window` 4-day grace and the
  `ALTER DEFAULT PRIVILEGES` narrowing (filed as separate tickets); SHOULD-FIX-4's
  `n_observations` daily-only caveat wording (not in this pass's four sweep topics).

### Gates (all green, final state)

```
$ .venv/bin/ruff check backend/app src db        → All checks passed!
$ .venv/bin/python -m pytest -q                  → 204 passed (assertions added to an existing
                                                   test; no new test functions, count unchanged)
$ shellcheck -x bin/*.sh                         → clean
$ bash bin/db_migrate.sh down --allow-destructive --target 000 && bash bin/db_migrate.sh up
                                                 → 004 applied in 336ms; status: 001–004 ok
```

Live verification after the cycle: `schema_migrations` = 4 rows, checksums `ok`,
`ck_paper_portfolios_strategy_kind` present with the definition above, **0 rows in all 13
evaluation tables**, `km-*` containers untouched, `data/market/` untouched, `rh_db_data` intact.
