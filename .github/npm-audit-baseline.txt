# Known high/critical npm advisories tolerated by .github/scripts/npm_audit_gate.sh.
# SHRINK-ONLY: prune ids as dependency upgrades land; NEVER add one to silence a new finding.
#
# EMPTY as of 2026-09-15, and it should stay that way. Every entry this file ever held traced to
# next@14 — ten against next itself and two against the postcss it pinned internally at 8.4.31,
# which no top-level bump could reach. next@16.3.5 cleared all twelve at once, which is what the
# previous header predicted would happen.
